<html>
    <head>
        <title>{{ title }}</title>
    </head>
    <body>
        <h1>{{ title }}</h1>
        <ul>
            {% for book in books %}
                <li>{{book.title}}, <b>{{book.author}}</b>, <c>{{book.genre}}</c>,
                <a href=books/{{books.index(book)}}>Show details</a>"
                </li>
            {% endfor %}
        </ul>
    </body>
</html>